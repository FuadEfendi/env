#!/bin/sh

SCRIPTNAME=$0
INTERNAL_SCRIPT=$1
shift;

WLS_ORACLE_HOME=

if [ -d "${WLS_ORACLE_HOME}" ]; then
  INTERNAL_SCRIPTPATH="${WLS_ORACLE_HOME}/oracle_common/common/bin"
else
  INTERNAL_SCRIPTPATH=`dirname "${SCRIPTNAME}"`
fi

"${INTERNAL_SCRIPTPATH}/${INTERNAL_SCRIPT}" "$@"
